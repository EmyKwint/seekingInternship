export class Interview {
    constructor(id, name, question, responses){
        this.id        = id;
        this.name      = name;
        this.question  = question;
        this.responses = responses;
    }
}